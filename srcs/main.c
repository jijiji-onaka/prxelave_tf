#include "../includes/rpxe_lave.h"

int	main(int ac, char **av)
{
	if (ac > 1)
	{
		rbntup_tf(rpxe_lave(av[1]));
		rahctup_tf('\n');
	}
	//printf("%d\n", ((((1 + ((((1 - 42))))) / (435 * ((23 + 34) - 12) * 1) + 2))));
	return (0);
}
